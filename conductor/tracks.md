# 🛤️ 宏观任务追踪注册表 (Tracks)

当前项目中所有的功能模块及基建路线大纲应记录于此库，以避免跨会话的迷失或重复造轮子。

## 🌟 在办 / 待办模块 (Active Tracks)

### 阶段一：核心基石 —— 传输页 (`transfer.html`)
> 攻坚路由骨架与传输页 UI，确认前端交互无误后，再打通后端信令与 WebRTC 通信。
- `[ ]` **功能 1：Vite + Vue3 全局骨架与前端路由映射**（集成左侧独立菜单导航，划分组件结构）。
- `[ ]` **功能 2：【前端静态验证】发送端与接收端 UI 实现**（纯前端 Mock 状态：六位邀请码展示、拖拽上传区域区、虚拟进度条、失败状态视图）。
- `[ ]` **功能 3：【前端静态验证】大文件切片与本地落盘 API 预调通**（不经过网络，仅在前端内存模拟 File/Blob API 边读边分片，并利用 File System Access API 将分片重组写入本地演示）。
- `[ ]` **功能 4：【后端接入】基于 Express + Socket.io 的信令服务搭建**（完成真正的邀请码短连配对，交换 WebRTC SDP）。
- `[ ]` **功能 5：【前后端联调】打通真实 P2P 传输与断线重连**（建立真实 RTCDataChannel，融合前期的静态 UI 状态与真实的传输流，实现 Ping-Pong 心跳与断点续传）。

### 阶段二：数据留存 —— 历史记录页 (`history.html`)
> 先完成丰富的数据表格 UI，再去挂载本地数据库。
- `[ ]` **功能 1：【前端静态验证】历史记录页 UI 静态渲染**（基于 Mock 数据渲染文件图标、对端设备名称、时间戳及成功/失败 Status Badge）。
- `[ ]` **功能 2：【前端静态验证】状态过滤与检索组件拆解**（实现 "全部 / 已发送 / 已接收 / 已失败" 标签页的悬浮与切换动画交互）。
- `[ ]` **功能 3：【逻辑联调】IndexedDB 传输流水持久化**（封装浏览器本地数据库存储逻辑，替换 Mock 数据，实现真实历史留痕与查询过滤）。

### 阶段三：网络拓扑 —— 设备管理页 (`devices.html`)
> 优先展现炫酷的网络拓扑卡片阵列。
- `[ ]` **功能 1：【前端静态验证】网格化设备卡片 UI 渲染**（设计并抽出 `DeviceCard` 静态组件，展示各种状态的假数据组合）。
- `[ ]` **功能 2：【逻辑联调】身份持久化与信任绑定**（结合 `localStorage` 或 Vuex/Pinia 记录已知对端设备，动态点亮在线状态）。
- `[ ]` **功能 3：【前后端探索性功能】局域网雷达扫描（探索性）**（尝试结合服务端或 WebRTC UDP 广播发现同网段设备免输码接入）。

### 阶段四：终极体验 —— 设置与个性化 (`settings.html`)
> 组装精致的设置表单控件，最后打通全局状态。
- `[ ]` **功能 1：【前端静态验证】设置页面精美表单控件绘制**（包含头像、输入框、iOS 风格 Toggle 开关与下拉菜单静态表现）。
- `[ ]` **功能 2：【逻辑联调】明暗主题（Light/Dark Theme）全量状态机打通**（结合 Tailwind CSS `dark:` 变体，使全局设置响应切换）。
- `[ ]` **功能 3：【逻辑联调】全局偏好数据绑定**（将表单数据与真实业务系统绑定，如“自动接收已信任设备”和应用“本机代号”更改）。

## ✅ 归档模块 (Archived Tracks)
- `[x]` Monorepo 骨架搭建 (pnpm workspaces)
- `[x]` 前后端框架与共享模型 (@repo/shared) 组装
- `[x]` Prisma ORM 数据库设计与启动脚本组装
- `[x]` `AGENT.md` v3 规范、AI 铁律与技能库注入
- `[x]` `conductor` 根目录库核心脉络初始化
