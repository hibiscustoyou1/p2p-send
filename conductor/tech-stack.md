# 🛠️ 详细技术栈规范 (Tech Stack)

该项目基于 PNPM Monorepo 构建，涵盖以下核心工作区与依赖版本：

## 1. 🏗️ 基础设施层 (Infrastructure)
- **包管理器**: `pnpm` (v10+，限定使用 workspaces 功能)
- **环境隔离**: `dotenv-cli` & 自定义加密流转 (`vault.js`)
- **Docker 容器化**: 项目根级已支持 `Dockerfile` 与快速构建/运行指令。

## 2. 💻 前端应用 (apps/client)
- **核心框架**: Vue 3 (v3.5+)
- **构建工具**: Vite (使用 `rolldown-vite`) + `vue-tsc` (严格的 DOM 层类型检查)
- **状态管理**: Pinia (v3+)
- **路由管理**: Vue Router (v4+)
- **UI 呈现与样式**: Tailwind CSS (v3+) + PostCSS + Autoprefixer
- **网络层**: Axios

## 3. ⚙️ 后端服务 (apps/server)
- **核心运行时**: Node.js (v20+)
- **Web 框架**: Express (v5.1+)
- **ORM 框架**: Prisma (v5.22+)
- **构建工具**: `tsup` & `esbuild`
- **开发工具**: `nodemon` + `tsx` (提供极速的 ts 运行与热重载)
- **跨域与安全**: `cors` 等。

## 4. 📦 共享模块 (packages/)
- **`@repo/shared`**: 业务类型定义 (Types) 与常量的单点真理 (Single Source of Truth)。支持 Browser 和 Node 环境双向完美导出。
- **`@repo/tsconfig`**: 严格分层的 TS 配置中心 (`base.json`, `node.json`, `dom.json`)。
